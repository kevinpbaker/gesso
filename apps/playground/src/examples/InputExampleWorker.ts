import { renderRoot } from '@gesso/framework';
import { InputExampleApp } from './InputExampleApp';

/** Render worker for the gestures page. The whole app lives here. */
renderRoot(InputExampleApp);
