import { renderRoot } from 'gesso-framework';
import { LayoutExampleApp } from './LayoutExampleApp';

/** Render worker for the layout page. The whole app lives here. */
renderRoot(LayoutExampleApp);
