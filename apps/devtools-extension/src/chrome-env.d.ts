/*
 * The `chrome.*` globals, for this app alone. The workspace's root
 * tsconfig names its `types` explicitly, so a package-level type
 * dependency has to be pulled in by a reference rather than found.
 */
/// <reference types="chrome" />
