# No Git LFS. The one video here is a generated gradient loop of about
# half a megabyte, and LFS on a file that size buys nothing while
# costing every contributor a working `git lfs` and every CI job and
# host an extra fetch step that silently yields a pointer file when it
# is missing. If a real video ever lands here, reconsider it then.
