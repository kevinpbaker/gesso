/**
 * The controlled-with-fallback contract moved into `gesso-framework`,
 * where an application component can reach it too; re-exported here so
 * the library's own imports and its API are unchanged.
 */
export { controlled, type ControlledOptions, type ControlledValue } from 'gesso-framework';
